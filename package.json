{
  "name": "vite-svelte",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:server": "vite build --ssr src/entry-server.ts --outDir dist/server"
  },
  "dependencies": {
    "svelte": "^3.31.2"
  },
  "devDependencies": {
    "rollup-plugin-svelte": "^7.1.0",
    "vite": "^2.0.0-beta.12"
  }
}
